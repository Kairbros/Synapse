# Configuración del build
[build]
  publish = "dist"  # o "build" dependiendo de tu configuración

# Redirects para SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
